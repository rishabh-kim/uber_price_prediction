# Render Deployment Configuration
# This file is used by Render to build and deploy the app

# Build command - installs dependencies (model.pkl is pre-trained and committed)
build = "pip install --upgrade pip setuptools wheel && pip install -r requirements.txt"

# Start command - runs the Flask app with gunicorn
start = "gunicorn app:app"

# Environment variables can be set in Render dashboard
